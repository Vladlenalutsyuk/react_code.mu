/*Ответ на вопрос №1: Типы компонентов Products, Product и ProductField
Исходя из паттерна разделения компонентов на контейнеры и презентационные:

Products - это компонент-контейнер (умный, толстый), потому что:

Содержит стейт с данными (prods)

Организует логику работы с данными

Использует дочерние компоненты для отображения

Не занимается непосредственно версткой

Product - это презентационный компонент (глупый, худой), так как:

Получает данные только через пропсы (name, cost)

Не содержит собственного стейта с данными

Отвечает только за отображение

Может быть легко переиспользован

ProductField (если он существует в предыдущих примерах) - также презентационный, если:

Просто отображает отдельное поле продукта

Не управляет состоянием данных

Ответ на вопрос №2: Стейты в презентационных компонентах
Презентационные компоненты могут содержать стейты, но только для управления своим внутренним поведением, а не для хранения данных приложения. Примеры таких случаев:

UI-состояния:

Флаги открытия/закрытия (например, раскрывающийся список)

Состояние hover/active у интерактивных элементов

Локальные значения полей ввода перед сохранением

Примеры из предыдущих уроков:

Компонент с переключаемым описанием (где был флаг show)

Редактируемое поле (локальный стейт для временного значения)

Анимационные состояния

Когда это допустимо:

Когда состояние касается только визуального представления

Когда состояние не нужно другим компонентам

Когда состояние является временным (например, значение input перед submit)

Когда это недопустимо:

Для хранения основных данных приложения

Для состояния, которым должны управлять родительские компоненты

Для данных, которые нужны другим частям приложения

Пример из предыдущих уроков - компонент с кнопкой "Показать/скрыть описание" содержал стейт show, который был нужен только для управления видимостью описания внутри этого компонента. Это допустимый случай использования стейта в презентационном компоненте.